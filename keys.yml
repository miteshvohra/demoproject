---
  - name: Deploy Spare boxes
    hosts: all
    remote_user: sccStudent
    gather_facts: false

      - name: Add ssh keys
        authorized_key:
          state: present
          user: sccStudent
          key: "{{ lookup('file', '~/.ssh/id_rsa.pub') }}"